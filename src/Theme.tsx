import { createTheme } from "@mui/material";

export const myTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#027bce"
    },
    secondary: {
      main: "#06d6a0"
    },
    background: {
      default: "#1C2128"
    }
  },
  typography: {
    fontFamily: "Sofia Regular"
  }
});

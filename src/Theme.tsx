import { createTheme } from "@mui/material";

export const myTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#027bce"
    },
    secondary: {
      main: "#06d6a0"
    }
  },
  typography: {
    fontFamily: "Sofia Regular"
  }
});
